__pycache__/
*.pyc
.venv/
venv/
.env
*.log

# Generated by scripts/build_digest.py. No longer committed -- the daily
# Actions job deploys docs/ straight to Pages as a build artifact instead,
# which is what removed the recurring rebase conflict this used to cause
# when a local `preview-digest` run and the bot both regenerated it. Still
# written to disk locally by every build/preview run, just not tracked.
docs/data/digest.json
